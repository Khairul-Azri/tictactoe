apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tictactoe.fullname" . }}-test-connection"
  labels:
    {{- include "tictactoe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tictactoe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
